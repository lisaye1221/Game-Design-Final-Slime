/// @description Insert description here


textbox_x = camera_get_view_x(view_camera[0]);
textbox_y = camera_get_view_x(view_camera[0] + (room_height - textbox_height));

// setup
if(!setup){
	setup = true;
	
	
}